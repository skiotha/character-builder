# Nagara Character Builder — Environment Variables

# Server
PORT=3000
LOCAL_ADDRESS=127.0.0.1
NODE_ENV=development

# Auth
NAGARA_DM_TOKEN=your-dm-token-here

# SSL (production only)
# SSL_KEY=/path/to/privkey.pem
# SSL_CERT=/path/to/fullchain.pem

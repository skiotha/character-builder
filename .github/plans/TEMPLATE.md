# Plan — <title>

**Status:** template (not an active plan — copy to `.github/plans/<name>.md` and replace the `<...>` placeholders).
**Owner:** <who>
**Trigger:** <the problem this plan solves / what prompted it>

## Goals

1. <goal>

## Non-goals

- <explicitly out of scope, to keep the work bounded>

## Steps

Break the work into iterations — each its own commit and confirmation stop,
each with a verifiable gate.

- **Step 1 — <name>.** <what changes>. **Done when:** <verifiable check>.
- **Step 2 — <name>.** <what changes>. **Done when:** <verifiable check>.

## Verification

- `npm run typecheck` clean; `npm test` green.
- <plan-specific checks, e.g. a regression grep>

## References to sweep on completion

Every code-side `TODO(<scope>)` that cites this plan is listed here, so the
cleanup obligation is "follow this checklist", not "remember to grep". An
empty list is fine — state it explicitly.

- _(none yet)_

## Progress

- [ ] Step 1 — <name>
- [ ] Step 2 — <name>
